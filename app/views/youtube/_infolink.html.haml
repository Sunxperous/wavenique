-with ||= nil
-additional ||= nil
-tags ||= nil
= link_to youtube_path(video_id) do
  .infolink
    .thumbnail
      = image_tag thumbnail_for(video_id)
    .infolink-text
      .primary= primary
      .tags= tags
      .with= with
      .additional= additional
